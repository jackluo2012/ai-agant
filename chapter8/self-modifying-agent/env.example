# LLM 配置说明
# 本项目使用项目根目录（ai-agant/.env）的统一配置
# 不需要在此文件中配置 API 密钥

# 在项目根目录 .env 中配置：
# API_KEY=your-api-key
# LLM_PROVIDER=kimi  # 或 openai, deepseek, aliyun 等
# LLM_MODEL=kimi-k3   # 可选

# 沙箱镜像配置（可选）
# SELF_MODIFY_SANDBOX_IMAGE=my-registry/self-modifying-sandbox:v1.0
