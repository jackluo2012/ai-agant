# LLM 配置说明

# 本项目的 LLM 配置统一在项目根目录的 .env 文件中管理。
# 此文件仅为示例参考，实际运行时请使用项目根目录的配置。

# ===== 项目根目录 .env 配置示例 =====
#
# # LLM 提供商配置
# API_KEY=your-api-key
# LLM_PROVIDER=kimi  # 或 siliconflow, doubao, deepseek 等
# LLM_MODEL=kimi-k3   # 可选，默认使用提供商推荐模型
# BASE_URL=           # 某些提供商需要（如阿里云）
#
# # 可选：覆盖默认模型
# COST_DEMO_MODEL=gpt-4o-mini

# 支持的 LLM 提供商：
# - Kimi (kimi): kimi-k3
# - SiliconFlow (siliconflow): Qwen/Qwen2.5-7B-Instruct
# - DeepSeek (deepseek): deepseek-chat
# - 阿里云 (aliyun): qwen-plus (需要 BASE_URL)
# - OpenAI (openai): gpt-4o-mini
