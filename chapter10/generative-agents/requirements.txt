# 核心依赖（openai、python-dotenv）由项目根目录虚拟环境统一提供，此处不再列出。
# 以下为上游源码运行所需的依赖（共享 .venv 已预装科学计算栈，版本以根目录为准）。
numpy>=1.25
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
# 上游遗留依赖（上游模块按需导入）
nltk>=3.6.5
# 离线单元测试
pytest>=8.0
